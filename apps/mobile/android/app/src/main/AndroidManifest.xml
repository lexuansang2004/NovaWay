<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Never actually declared until now — apps/mobile has only ever run via
         `flutter test` (fakes, no real permission prompt) and `flutter run -d
         chrome` (web target, browser Geolocation permission, not this
         manifest). A real Android build/install would have the GPS
         permission request silently fail (OS won't prompt for an
         undeclared permission) and, in a release build specifically (debug/
         profile variants happen to carry INTERNET already, but only for
         Flutter's own dev tooling — see src/debug and src/profile), every
         network call would fail outright. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- geolocator_location_source.dart only ever calls the base
         Geolocator.getPositionStream (no AndroidSettings/foreground-service
         config), so this app never runs location tracking while backgrounded
         — no FOREGROUND_SERVICE_LOCATION/POST_NOTIFICATIONS needed, matching
         what the code actually does rather than the plugin's full example. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
