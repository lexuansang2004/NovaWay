{
  "name": "novaway",
  "private": true,
  "version": "0.0.0",
  "description": "NovaWay monorepo — backend (NestJS), web (React), mobile (Flutter, managed separately), shared-types.",
  "scripts": {
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.14.0"
}
